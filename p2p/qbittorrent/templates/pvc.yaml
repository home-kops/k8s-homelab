apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config
  namespace: p2p
spec:
  storageClassName: nfs-sc
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
